log

__pycache__
build
dist
.wheelhouse
*.egg-info
.ipynb_checkpoints

*.tar.gz
.debug_env

.vscode/extensions.json
.vscode/tasks.json

/.env
/.venv

tofdaq
/.runtime/env/*
# Downloaded/built demo bundles (large; fetched on demand by `mascope demo`)
/.runtime/demo/
# Per-worktree dev-stack instance registry (machine-local, see `mascope instance`)
/.runtime/instances.json
/.runtime/instances.json.*

# MkDocs build output
/site/

# Python third-party attributions (tooling/third-party-notices.py). The backend
# serves them from its runtime home (MASCOPE_PATH), which is this checkout only
# where MASCOPE_PATH points at it; the images generate their own at build time
/THIRD_PARTY_NOTICES.txt
/DOCS_THIRD_PARTY_NOTICES.txt
